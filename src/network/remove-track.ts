import type { Simulation } from "../simulation/mod.ts";

/** Remove a long track
 *  if it can be replaced by two other tracks
 *  that each are shorter
 *  and combined length is maximum 1.5 of the long track */
export function removeTracks(game: Simulation): boolean {
  // TODO
  return true;
}
